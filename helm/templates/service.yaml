apiVersion: v1
kind: Service
metadata:
  name: {{ include "pragma-monitoring.fullname" . }}
  labels:
    {{- include "pragma-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pragma-monitoring.selectorLabels" . | nindent 4 }}